# Architecture Decision Records

One short file per non-obvious decision, numbered in order (`0001-...md`, `0002-...md`).
Each records the decision, the context that forced it, and the alternatives rejected -
so the reasoning survives even when the code changes. Optional; delete this folder if
the mod never needs it.
